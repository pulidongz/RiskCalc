import React, { useEffect, useState, useRef } from "react";
import {
    Avatar,
    Box,
    Button,
    CssBaseline,
    CardMedia,
    Container,
    Image,
    Grid,
    Link,
    Paper,
    Snackbar,
    TextField,
    Typography,
} from "@material-ui/core";

import { makeStyles } from "@material-ui/core/styles";

export default function ResultPage(props){
    return(
        <Grid>
            <Typography>Construction Delays</Typography>
        </Grid>
    );
}